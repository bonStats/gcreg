#' Search for boundary of permissible region between current value and desired value.
#' 
#' @param cur numeric .
#' @param aim A number.
#' @return The sum of \code{x} and \code{y}.
#' @examples
#' add(1, 1)
#' add(10, 1)

line_search <- function(cur, aim,
                        EPS = 1e-05 ) {
  1
}